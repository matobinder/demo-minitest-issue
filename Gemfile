# frozen_string_literal: true

source 'https://rubygems.org'
ruby '2.4.1'

group :test do
  gem 'ci_reporter'
  gem 'ci_reporter_minitest'
  gem 'minitest'
  # gem 'minitest-reporters', '1.1.17' # This works
  gem 'minitest-reporters', '1.1.18' # This doesn't
  gem 'rake'
end
